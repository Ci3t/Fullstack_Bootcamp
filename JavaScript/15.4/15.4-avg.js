function calcAverage (arr){
    // var sum ; // need to give = 0 to be Num
    var sum = 0; // need to give = 0 to be Num
    for ( var i = 0 ; i < arr .length; i ++ ){
    sum += arr [ i ];
    } return sum ; //? break here sum is NaN
    }
    calcAverage ([ 85 , 90 , 92 ]);

    //! used nodejs debug sum was not defined need to give it = 0 to make it Num